name: watchlist
description: A better Watchlist for movies and TV Series

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4
  macos_ui: ^0.10.0+1
  flutter_dotenv: ^5.0.2
  isar: 2.2.1
  isar_flutter_libs: 2.2.1
  path_provider: ^2.0.9
  json_serializable: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  isar_generator: 2.2.1
  flutter_lints: ^1.0.4
  build_runner: ^2.1.7
  build_web_compilers: ^3.2.2

flutter:
  assets:
    - .env